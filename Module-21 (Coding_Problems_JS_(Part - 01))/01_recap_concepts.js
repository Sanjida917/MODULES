
/**There are three types of variable in Js We can declare :
 * 
 * 1) Var    --> Not so usefull. Very few times may be use.
 * 
 * 2) Let    --> Can change the value of the variable's
 * 
 * 3) Const  -->  Can't Change the value of the variable's.
 * 
 */


/**
 * Const Array Variable :
 * -->We can change the Properties of the array but can't replace with a new value.
 * -->we can't set as the new value of the const variable but we can Push() or Pop() in a const value.And Also can Update the values.
 */

/**
 * Const Objects Variables:
 * 
 * We cannot set the new value of an const object but ,
 * -->We can change the Properties of the Object but can't replace with a new value.
 * -->can Update the object properties/Values
 * -->can add new property
 * -->can remote the property/values
 * -->Also canmodify the properties.
 * 
 */





//-----------------------------------Recap Variables-------------------------------------

/**
 * The are two main types of Variables :
 * 
 * 1) Primitive-Type Variable      --> Null, Undefined, String's, Number's, Boolean's etc.
 * 2) Non-Primitive Type Variable  -->  Object's, Array's .
 * 
 */